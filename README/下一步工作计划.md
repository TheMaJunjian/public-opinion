# 公论下一步工作计划

> 日期：2026-07-28
> 最近复查：2026-07-28，基于全部文档与源码对照，及本轮 7 项工程改动完成后更新。

---

## 1. 当前状态：协议原型已闭环

经过本轮工作，系统已从"中心化 Web 原型"收束为**可演示、可审计、可自治理的协议原型**。

### 核心能力矩阵

| 能力 | 状态 | 治理可改 |
|------|:--:|:--:|
| 统一消息模型（6 种 kind）+ 17 种关系 + 图谱/列表双视图 | ✅ | — |
| 押注 + BetPool + 结算 + 推翻 + clawback + 负债冻结 | ✅ | ✅ |
| 审计日志 + replay/verify 复算 | ✅ | — |
| 治理提案 → 结算 → 新 RuleVersion 生效 | ✅ | ✅ |
| 手续费 → RevenuePool → 手动分配 | ✅ | ✅ |
| 标签统计 API + 清爽视图 subType 过滤 | ✅ | ✅ |
| 导出含完整经济数据（ROUND/ROUND_RESULT payload） | ✅ | — |
| Ed25519 签名：注册生成密钥 → 自动签名 → 服务端验签 | ✅ | — |
| 关系创建预览提示（DISAGREE 容器/标注时显示后果） | ✅ | — |

### 测试基线

```bash
cd backend && npm run build && npm test   # 11 套件 / 155 测试
cd frontend && npm run build && npm test  # 10 文件 / 279 测试
```

### 已修复的历史问题

- 前端生产构建失败、后端测试失败、前端 lint 失败
- replay/verify 复算脚本
- 审计日志 entityId 回补反模式、mock API 不同步
- BetPool 复合键语义、e2e 测试规则同步
- TopicDetailPage 部分拆分
- 命名不一致："聚焦模式"→"清爽视图"

---

## 2. 工程基线

```bash
# 启动开发环境
cd backend  && docker-compose up -d postgres && npm run dev   # :3000
cd frontend && npm run dev                                      # :5173

# 提交前验证
cd backend  && npm run build && npm test
cd frontend && npm run build && npm test
```

| 种子默认 | 值 |
|----------|----|
| 用户 | MaJunJian / test123456 |
| 注册奖励 | 2000 点 |
| 自押 | 10 点 |
| 手续费 | 1 点（入 RevenuePool） |
| 结算权限 | anyone |
| 创建者奖励 | 20% |
| 收入分配 | manual（手动触发） |

所有参数可通过治理提案修改。

---

## 3. 本轮改动（2026-07-25 ~ 2026-07-28）

| # | 功能 | 关键文件 |
|---|------|---------|
| 1 | 治理→RuleVersion | events.ts, settlement.api.test.ts |
| 2 | 手续费入池 | events.ts |
| 3 | 收入分配 API | revenue.ts, seed.ts |
| 4 | 标签统计 API | tags.ts（新建）, useCleanView.ts |
| 5 | 导出补全经济字段 | export.ts |
| 6 | Ed25519 签名 | middleware/auth.ts, AuthContext.tsx, RegisterPage.tsx, client.ts |
| 7 | 关系创建预览提示 | TopicDetailPage.tsx, TopicRightPanel.tsx |

---

## 4. 未完成

| # | 事项 | 优先级 |
|---|------|:--:|
| 1 | 阅读曝光（ReadEvent） | 低 |
| 2 | 结论卡（可用 SUMMARY 替代） | 低 |
| 3 | 搜索 API（清爽视图已覆盖） | 低 |
| 4 | 用户页（StanceHistoryPanel 已覆盖） | 低 |
| 5 | 兑换/准备金 | 远期 |
| 6 | 独立审计节点 | 远期 |

---

## 5. 下一步

1. **演示**：按标准演示剧本走完整流程
2. **公论治理公论**：在系统内发起治理提案
3. **持续改进**：根据使用反馈改进 UI/交互
