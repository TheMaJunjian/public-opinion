# 实施路线图（SRS 版）

### Phase 0：基础设施补齐（约 2-3h） ✅ 已完成

| # | 任务 |
|---|------|
| 0.1 | auth 注册改为走 applyEvent（统一写路径） |
| 0.2 | events.ts 加 $transaction（原子性） |
| 0.3 | User 加 publicKey 字段（去中心化预留） |
| 0.4 | seed RuleVersion v1 初始参数 |

### Phase 1：贡献点与账本（约 6-8h） ✅ 已完成

| # | 任务 |
|---|------|
| 1.1 | PointAccount + PointTransaction 模型 |
| 1.2 | Balance + debt_frozen 模型 |
| 1.3 | LedgerEntry 模型（全部 entry_type） |
| 1.4 | 贡献点铸造/解锁初始规则 |
| 1.5 | GET /api/points/balance + transactions API |
| 1.6 | GET /api/rules/current API |
| 1.7 | PointsBadge 前端组件 |
| 1.8 | 测试 |

### Phase 2：押注与资金池（约 12-14h） ✅ 已完成

| # | 任务 |
|---|------|
| 2.1 | Stake + BetPool 模型 |
| 2.2 | POST /api/messages/{id}/stakes（扣款+锁定） |
| 2.3 | BetPool 同步更新（locked_pro/locked_con） |
| 2.4 | GET /api/messages/{id}/stakes（统计） |
| 2.5 | 发消息自动自押 PRO：创建 TEXT 消息时，自动从创建者 Balance 扣款（金额=用户指定的自押金额，默认 1），锁定到 BetPool.locked_pro |
| 2.6 | 发 RELATION 消息同理自动自押 PRO |
| 2.7 | 更新 seed RuleVersion v1 参数：加入 selfStakeOnCreate（默认 1 点） |
| 2.8 | 发送按钮旁行内押注金额控件（替换独立 StakeInput 面板） |
| 2.9 | 移除 TopicDetailPage 独立 StakeInput 面板，押注入口统一到"发送=押注"和"赞同/反对=押注" |
| 2.10 | 测试：余额不足、冻结用户拦截、最小金额、发消息自动自押、自押后 BetPool 非空 |
| — | ⚠ AGREE/DISAGREE 对目标消息的押注映射（PRO→目标 CON→目标）推迟到 Phase 3 |

### Phase 3：结算轮次与分账（约 12-14h） ✅ 已完成

| # | 任务 |
|---|------|
| 3.1 | SettlementRound + VoteStake 模型 |
| 3.2 | POST /api/messages/{id}/rounds（并发约束） |
| 3.3 | POST /api/rounds/{id}/votes（可多次投票） |
| 3.4 | 结算算法：权重判定 + 分账 |
| 3.5 | POST /api/rounds/{id}/close-and-settle |
| 3.6 | LedgerEntry: SETTLEMENT_PAYOUT 生成 |
| 3.7 | SettlementPanel + RoundHistory 前端 |
| 3.8 | 测试：TRUE/FALSE/UNKNOWN、平局、并发冲突 |

### Phase 4：推翻与回滚（约 10-12h） ✅ 已完成

| # | 任务 |
|---|------|
| 4.1 | Clawback 算法：查 previous_round → 冲销分录 |
| 4.2 | debt_frozen 自动判定与拦截 |
| 4.3 | 负债用户权限拦截（押注/投票/发起结算 403） |
| 4.4 | 推翻链前端展示 |
| 4.5 | 测试：扣回不足→负债、还清→解冻、连续推翻 |

### Phase 5：立场历史与证据 + UX 优化（约 12-16h） ✅ 已完成

> **设计决策**：证据不建独立 `EvidenceRef` 表，而是复用现有 REFERENCE 关系 + 二级附加标签 `证据`。
> 每条消息（文本/关系）都可独立押注/投票/结算，证据链本身即立场链。
> **2026-06-18 新增**：投票统一到 AGREE/DISAGREE 关系消息（VoteStake 表不再写入），
> 装饰双击触发立场路径高亮（AGREE/DISAGREE 边 → 源文本消息 → REFERENCE(证据) 目标）。

| # | 任务 |
|---|------|
| 5.1 | REFERENCE 二级附加"证据"、"自定义"标签 |
| 5.2 | GET /api/users/{id}/stances（含 REFERENCE(证据) 条目） |
| 5.3 | StanceHistoryPanel（展示表态 + 证据引用） |
| 5.4 | GraphView 立场路径高亮（含 REFERENCE(证据) 边金色高亮） |
| 5.5 | UX：贡献点记录双击 → 消息原地定位 ✅ 已完成 |

### Phase 6：一切皆消息 + 治理与收入（约 16-22h）

> **设计原则**：一切用户主动操作都是一条消息。治理提案是消息，投票是对治理消息的 AGREE/DISAGREE，结算是 ROUND 消息，代码变更是 CODE 消息。前端只做消息的展示和交互——非线性视图、清爽视图、焦点模式都是同一批消息的不同投影，新增显示模式不改变消息模型。

| # | 任务 |
|---|------|
| 6.1 | **废弃独立押注 API**：`POST /api/messages/:id/stakes` 移除，押注仅通过发消息（自押）和 AGREE/DISAGREE 投票消息产生 |
| 6.2 | **Round 消息化**：`Message` 新增 `kind: 'ROUND'`，发起结算 = 创建 ROUND 消息（同步写 `SettlementRound` 表，结算引擎不变）；关闭结算追加 `kind: 'ROUND_RESULT'` 消息 |
| 6.3 | **治理提案消息**：`kind: 'GOVERNANCE'`，提案是文本消息的子类型 → 讨论/投票(AGREE/DISAGREE) → 结算(ROUND消息) → 通过后进入待应用状态 |
| 6.4 | **CODE 消息**：`kind: 'CODE'`，承载程序代码变更，结算通过后自动构建/部署，RuleVersion 关联 CODE 消息，实现「规则修改可追溯、可讨论、可推翻」 |
| 6.5 | **清爽视图**：利用现有 标注 二级附加标签（推荐/冷藏/自定义），冷藏标记达到阈值 T（默认 5）时折叠该消息分支；清爽视图与焦点模式同架构——前端按标签阈值过滤消息子集，不新建数据模型 |
| 6.6 | **RevenuePool + RevenueDistribution**：收入池接收用户充值与外部收入，按规则自动分配给贡献点持有者（类似分红）；分配结果写入 LedgerEntry，不产生新消息类型 |
| 6.7 | **AuditLogView / RevenuePanel / GovernancePanel**：前端审计日志、收入面板、治理面板 |

### Phase 7：渐进式去中心化

```text
阶段一：事件化 Web 原型（与 Phase 1-6 并行）✅ 已就位
    ↓
阶段二：公开复算（replay 脚本）
    ↓
阶段三：独立审计节点
    ↓
阶段四：关键状态上链
```

---

## 工作量总结

| 阶段 | 内容 | 预估 |
|------|------|------|
| Phase 0 | 基础设施补齐 | 2-3h |
| Phase 1 | 贡献点与账本 | 6-8h |
| Phase 2 | 押注与资金池（含发消息自押） | 12-14h |
| Phase 3 | 结算轮次与分账 | 12-14h |
| Phase 4 | 推翻与回滚 | 10-12h |
| Phase 5 | 立场历史与证据 + UX 优化 | 12-16h |
| Phase 6 | 一切皆消息 + 治理与收入 | 16-22h |
| **合计（Phase 0-5）** | **可演示争议结算核心** | **约 54-67h** |

---

## 后续重构计划

> 以下是在 Phase 0-4 实施过程中累积的技术债，建议在 Phase 5 或之后逐步处理。

| # | 问题 | 影响 | 建议 |
|---|------|------|------|
| R1 | `TopicDetailPage.tsx` 3700+ 行 | 维护困难，改一处易引发连锁 bug | 拆分：`useFocusManager`、`useComposer`、`useSettlement` 等 hook |
| R2 | `focusExitKey` 模式（5 处手动 `setFocusExitKey(k+1)`） | 补丁式重挂载，非响应式 | `StructureView` 内部用 `useEffect` 监听 `focusIds` 变化并重算布局 |
| R3 | ~~Stake / VoteStake 分离~~ | ~~用户需两次操作（押注+投票）~~ | ✅ 已合并：投票 = 发送无文本 AGREE/DISAGREE 关系消息，Stake 即 Vote（2026-06-18） |
| R4 | `PointTransaction.type` 枚举化 | 字符串散落各处，易拼写错误 | 抽取为 `PointTransactionType` enum |
| R5 | 事件重放脚本缺失 | 去中心化 Phase 7 硬前置 | 从 AuditLog 编写 replay 脚本，验证全部状态可复算 |
| R6 | `User.publicKey` 未使用 | 去中心化签名验证无基础 | Phase 7 前实现消息/押注签名验证 |
| R7 | ~~前端测试覆盖率不均~~ | ~~layout/graphView 10 个失败~~ | ✅ 已修复（2026-06-16） |

---
